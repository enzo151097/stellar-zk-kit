[package]
name = "{{circuit.name}}_verifier"
version = "0.0.0"
edition = "2021"
publish = false

[lib]
crate-type = ["lib", "cdylib"]
doctest = false

[dependencies]
soroban-sdk = { version = "26.1.0" }

[dev-dependencies]
soroban-sdk = { version = "26.1.0", features = ["testutils"] }
